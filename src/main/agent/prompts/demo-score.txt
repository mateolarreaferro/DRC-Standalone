# Demo score (default for every new CSD)

Showcase the instrument musically (~8–12 s), not one held note.
Unless the user asks for one note only or a specific melody:

1. Short ascending scale (4–5 notes)
2. One arpeggio (4 notes)
3. Brief ostinato (4 notes)
4. Closing chord (3–4 notes, 1.5–2 s)

Score rules (HARD):
- `iFreq = cpsmidinn(p4)` in instr 1; score p4 = MIDI note (60 = middle C), p5 = amp 0.15–0.28
- Use `linsegr 0, 0.01, 1, p3 - 0.02, 0.001, 0.01, 0` for note envelopes — NOT `expseg` with `p3 *` math (causes INIT ERROR on short notes)
- Put `reverbsc` inside instr 1 — do NOT schedule `i 99` unless you define `instr 99`
- Never score an instrument number that is not defined in the orchestra
- Numbered instruments only: `i 1` not `i "Name"`

Safe demo score (copy and adapt):

```
<CsScore>
i 1 0.00 0.40 60 0.22
i 1 0.40 0.40 62 0.22
i 1 0.80 0.40 64 0.22
i 1 1.20 0.40 67 0.22
i 1 1.60 0.40 72 0.24
i 1 2.20 0.30 60 0.24
i 1 2.50 0.30 64 0.24
i 1 2.80 0.30 67 0.24
i 1 3.10 0.30 71 0.24
i 1 3.60 0.18 67 0.20
i 1 3.78 0.18 64 0.20
i 1 3.96 0.18 67 0.20
i 1 4.14 0.18 72 0.20
i 1 4.80 1.80 60 0.18
i 1 4.80 1.80 64 0.16
i 1 4.80 1.80 67 0.14
i 1 4.80 1.80 72 0.12
</CsScore>
```

# FM bell (copy this instr 1 when asked for a bell)

`foscili` is ONE opcode: `aSig foscili xamp, kcps, xcar, xmod, kndx, ifn` — NOT two foscili lines.

```
giSine ftgen 1, 0, 4096, 10, 1

instr 1
  iFreq = cpsmidinn(p4)
  iAmp  = p5
  kEnv  linsegr 0, 0.005, 1, p3 - 0.01, 0.001, 0.005, 0
  kIdx  linsegr 9, 0.02, 9, p3 * 0.25, 2, p3 * 0.75 - 0.02, 0.2
  aSig  foscili kEnv, iFreq, 1, 3.51, kIdx, giSine
  aL, aR reverbsc aSig * iAmp, aSig * iAmp, 0.84, 9000
  out(aL, aR)
endin
```

Bell timbre: `xcar=1`, `xmod=3.51`, `kIdx` starts high and decays via `linsegr`.

# FM woodblock / percussion hit (copy when asked for woodblock, clave, block — use fmpercfl)

From **Csound Manual fmpercfl** — standard kc1/kc2 FM percussion. Decay `kc2` with `expsegr`. For MIDI add `massign 0, 1` and `cpsmidinn(p4)` / `ampmidi`.

```
massign 0, 1

instr 1
  iFreq = cpsmidinn(p4)
  iAmp  = p5
  kc1   = 5
  kc2   expsegr 5, 0.002, 4.2, 0.07, 0.75, 0.015, 0.75
  aSig  fmpercfl iAmp * 0.55, iFreq, kc1, kc2, 0.01, 9
  outs  aSig, aSig
endin
```

See golden `fm_woodblock_midi_starter.csd` and catalog `csoundmanual-fmpercfl`. For modal wood (not FM), see `zuccobook-cap-10-6-wood`.

# FM bass / pluck bass (copy when asked for bass)

Use `foscili` + `expsegr` pluck envelope. For ping-pong echo, use `gaEcho` + `instr 99` with `vdelay3` (see golden pluck_bass_starter). Score MUST include `i 1` note lines — never `f 0` hold only.

```
instr 1
  iFreq = cpsmidinn(p4)
  iAmp  = 0.6
  kCarEnv = expsegr(1, 0.001, 1, 0.2, 0.0001)
  kModEnv = expsegr(1, 0.0005, 0.3, 0.08, 0.01, 0.3, 0.0001)
  kModIndex = kModEnv * 8
  aFM = foscili(kCarEnv * iAmp, iFreq, 1, 2.01, kModIndex, 1)
  aFat = butterlp(aFM, iFreq * 6)
  outs aFat, aFat
endin
```

Bass demo score (MIDI 36–48):

```
<CsScore>
i 1 0   1.5 36 0.6
i 1 1   1.5 43 0.55
i 1 2   1.5 48 0.5
i 1 3   2.0 36 0.55
</CsScore>
```
